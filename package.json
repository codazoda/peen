{
  "name": "ollama-code-min",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Node CLI agent for Ollama with streaming + run(cmd) tool.",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node cli.js"
  }
}
