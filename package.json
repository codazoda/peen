{
  "name": "peen",
  "version": "0.1.17",
  "private": true,
  "type": "module",
  "description": "Minimal Node CLI agent for Ollama with streaming + run(cmd) tool.",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node peen.js"
  }
}
