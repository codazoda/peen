Your tool call was invalid or used an unsupported tool name.
Re-emit ONLY one corrected one-line JSON tool call.
Rules:
- Use "tool":"run"
- Use a single line of JSON with double quotes for keys/values
- Escape any double quotes inside cmd, or use single quotes in the shell command
- Do not include extra braces, extra lines, or any other text
Example:
{"tool":"run","cmd":"find . -type f -name '*.js' -print"}
